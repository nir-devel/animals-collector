import chai, { expect } from "chai";
// import { clone, cloneDeep, groupBy, rest } from "lodash-es";
import { clone, cloneDeep, defaults } from "lodash-es";

// import * as Restaurant from "../restaurant.js";

chai.should();
chai.expect();

describe("Bankist", () => {
  it("Test 1!", () => {});
  expect(2 === 2).to.equal(true);
});
